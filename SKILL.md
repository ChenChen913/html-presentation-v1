---
name: html-presentation
description: >
  生成可在浏览器中直接运行的单文件 HTML 演示文稿（幻灯片）。
  当用户提到"演示文稿"、"幻灯片"、"slide deck"、"presentation"、"HTML slides"、
  "做一个演示"、"做一个 PPT"（但想要 HTML 格式），或提供大纲/文字稿想转成演示时立即触发。
  输出为零依赖单文件 HTML，可直接用浏览器打开演示，无需安装任何工具。
  本 skill 的核心竞争力：20 套 × 10 页的高质量模板库（液态玻璃家族：经典八套 + 新增四色；
  另有苹果发布会式 Keynote 变体六款：浅色奶油/青瓷/冰川 + 深色黑场/蓝调/极光；
  以及高级排版双变体：美术馆画册暖白纸朱砂 + 东方卷轴墨绿鎏金竖排）、
  严格的避雷规则与统一翻页引擎，确保输出排版精准、设计专业、断网可用，
  彻底避免 AI 生成演示文稿的常见烂大街问题。
---

# HTML 演示文稿 Skill

## 核心原则

本 skill 有三个同等重要的支柱：

1. **模板优先** —— 不要从零写 HTML，从 20 套已验证模板中选一套作底稿。
2. **零依赖** —— 断网双击就能完整运行：无 CDN、无外部字体、无图表库、无图标库。
3. **避雷规则** —— 「不要做什么」与「要做什么」同等关键。

---

## 工作流程

### Step 1：收集信息

从对话中提取（能推断的不要问）：

- **主题/内容**：用户提供的大纲、文字稿或一句话描述
- **页数**：默认按所选模板的 10 页标准结构走；用户指定则按指定
- **风格**：若用户未指定，展示下方风格菜单让用户选（或用户说"随便"时按题材推断）
- **用途**：汇报、教学、发布、演讲（影响模板与密度取舍）

**风格菜单（映射到模板文件，全库同属液态玻璃家族，按色彩情绪选）：**

```
经典八套（原版玻璃基因）
 A. 液态玻璃经典  → template01   产品发布 / 通用（紫蓝渐变 + 薄荷绿）
 B. 流光毛玻璃    → template02   品牌与视觉类提案（四色流动 + 渐变字）
 C. 莫兰迪矿物    → template03   生活方式 / 人文分享（米白 + 晨雾绿）
 D. 学术液态      → template04   教学课件 / 工作坊（银灰 + 灰湖蓝）
 E. 自然生机      → template05   社区 / 环保 / 轻松主题（白底四光斑）
 F. 全要素图表    → template06   业务复盘 / 数据汇报（含 SVG 图表）
 G. 全能双主题    → template07   内部产品 / 需要深浅色切换
 H. 代码旗舰      → template08   技术分享（代码高亮 + 图表 + 双主题）

新增成员（同基因新色彩世界）
 I. 暖杏陶土玻璃  → template09   品牌故事 / 餐饮社区（暖调亲和）
 J. 薄荷清风玻璃  → template10   户外 / 生活方式企划（清新活力）
 K. 樱粉暮蓝玻璃  → template11   婚礼 / 活动 / 仪式感提案（浪漫柔美）
 L. 落日鎏金玻璃  → template12   文化展览 / 出版人文（温润典雅）

Keynote 发布会变体（全屏大字 + 玻璃光影，支持滚轮翻页）
 M. 奶油暖秋玻璃 Keynote → template13   新品发布会 / 产品介绍（奶油米黄 + 焦糖橙）
 N. 黑场发布会 Keynote   → template14   旗舰发布 / 颁奖夜 / 年度演讲（近黑 + 蜂蜜金）
 O. 晨雾青瓷玻璃 Keynote → template15   茶饮 / 健康生活方式发布（晨雾青瓷 + 青瓷绿）
 P. 深空蓝调玻璃 Keynote → template16   科技 / AI 产品发布（深空蓝黑 + 星芒蓝）
 Q. 冰川晨雾玻璃 Keynote → template17   环保 / 气候 / 硬科技发布（雾蓝灰 + 冰川蓝）
 R. 极光夜航玻璃 Keynote → template18   航天 / 通信 / 夜间主题发布（深夜蓝 + 极光青雾紫）

美术馆画册与东方卷轴变体（宋体/楷体高级排版 + 玻璃画框/玻璃月，支持滚轮翻页）
 S. 美术馆画册（暖白纸）  → template19   展览画册 / 文化特刊 / 品牌年册（暖白纸 + 朱砂红，左对齐编辑排版）
 T. 东方卷轴（墨绿鎏金）  → template20   东方美学 / 茶事雅集 / 人文刊物（墨绿深潭 + 鎏金，中轴竖排卷轴、章回汉字、玻璃月、印章）
```

### Step 2：选定模板，复制为底稿

1. 读取 `references/TEMPLATES.md` 的目录，确定 1 套模板文件。
2. **复制整个文件**作为输出底稿，保留其全部 CSS、HTML 骨架与 `<script>` 引擎。
3. 需要读规则时按顺序：先 `references/AVOID_LIST.md`（不能做什么），再
   `references/LAYOUT_RULES.md`（怎么做），需要组件细节再查 `references/TEMPLATES.md`。

### Step 3：逐页改写内容

- 按模板的 **10 页标准结构**（封面 / 目录 / 背景问题 / 核心理念 / 关键数据 / 对比分析 / 实施路径 / 深入细节 / 总结展望 / 结尾）逐页替换文字。
- **换内容不换结构**：原页 4 条要点就写 ≤4 条；原页是表格就还是表格。
- 用户页数不足 10 页：优先删「对比分析」「深入细节」这类可缺页；扩到 10 页以上时按标准结构复制组件页。
- 主题色调整只动 `:root` 变量（含暗色块），不逐处替换。
- 内容密度红线：单页要点 ≤5 条、正文 ≤80 字、标题 ≤14 字且**主标题必须单行**（详见 LAYOUT_RULES 第五节）。

### Step 4：生成与自检

输出单文件 HTML 到用户指定的输出目录（未指定则用当前会话约定的输出目录）。

交付前必须完成（详见 AVOID_LIST 第九节清单）：

1. 零依赖扫描：全文无 `http://`、`<link`、`<script src`、`@import`。**注意 base64 内部可能含 `//` 或 `http` 子串造成假阳性，扫描前先 `re.sub` 把 `base64,…` 整个载荷替换掉**。
2. 溢出检测：用无头浏览器逐页激活并测量（可用仓库 scripts/qa_overflow.js 的思路），任何溢出回炉改文案。
3. 交互冒烟：键盘 / 触摸 / 按钮 / `#n` 深链 / F 全屏；双主题模板切换后对比度检查。
4. （玻璃系 01-12 推荐）字体生效验证：用 canvas `fillText` + `getImageData` 算 FNV-1a 哈希，**不要用 `document.fonts.check()`**——它对任何 family 都返回 true，验不出回退。详见 LAYOUT_RULES 字体节。

### Step 5：告知用户

```
文件已生成：presentation.html
用浏览器直接打开即可演示。

操作方式：
← → / 空格：翻页        Home / End：首尾页
F：全屏                 ESC：退出全屏
（13-20 另支持鼠标滚轮翻页）
手机上左右滑动翻页；地址栏 #5 可直达第 5 页
```

---

## 关键技术规范

- `references/AVOID_LIST.md` —— 禁止项 + 玻璃系受控例外 + 自检清单（⚠️ 必读）
- `references/LAYOUT_RULES.md` —— 画布、字体、密度预算、组件、SVG 图表、代码高亮规范
  - §4 字体系统 包含**首选本地字体栈**、**可选内联子集**（解决跨设备字体不可用）、
    三档字符集实测体积表（A=330 KB / B=510 KB / **C 常用 3755 字=1.31 MB**）与
    **10 条常见坑清单**（CDN 端点 / base64 假阳性 / `@media` 排除 / CJK 字宽陷阱 / Playwright 退出等）
- `references/TEMPLATES.md` —— 20 套模板目录 + 10 页标准结构 + 引擎说明
- `references/templates/template01-20.html` —— 模板本体（即文档即示例；玻璃系 01-12 已内联 LXGW WenKai Screen 字体子集，运行期仍零外网）

读取顺序：AVOID_LIST → LAYOUT_RULES → TEMPLATES → 打开所选模板文件动手改。

---

## 输出文件结构要求

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>主题 — 模板名模板</title>
  <style>/* 全部 CSS 内联；:root 变量 + 重置 + .slide + 组件 + 控件 + 响应式 + reduced-motion */</style>
</head>
<body>
  <!-- 液态玻璃家族：环境光斑/渐变 + .presentation-container 玻璃舞台 -->
  <section class="slide active">…</section> × N
  <!-- 控件：data-action 按钮 + #pageCounter；进度条 #progressFill -->
  <script>/* 引擎内联：键盘/触摸/按钮/hash/全屏，见模板现成实现 */</script>
</body>
</html>
```

**零依赖原则**：不使用任何 CDN、外部字体 URL、外部图片、图标库、图表库。所有资源内联或走本地字体栈。

---

## 迭代修改

用户反馈后：

- 改单页：定位 `#slide-N`（或第 N 个 `.slide`），局部修改，不重写整个文件
- 换风格：换一套模板做底稿搬运内容，或只改 `:root` 变量
- 加页：复制结构最接近的现有页，改文字，更新引擎无需任何改动（页数自动）
- 减页：整页删除即可，进度条与页码自动适配
- 每次修改后重跑 Step 4 自检
